module.exports = {
    purge: [],
    theme: {
        fontFamily: {
            title: ['Arvo', 'serif'],
            body: ['Lato', 'sans-serif'],
        },
    },
    variants: {},
    plugins: [require('@tailwindcss/typography')],
}
